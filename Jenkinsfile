pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo "Hello World Michel!"
                sh 'python --version'
                sh 'python Deloitte.py'
            }
        }
    }
}